diff --git a/core/rtw_xmit.c b/core/rtw_xmit.c
index cbab0a3..b13b9f0 100644
--- a/core/rtw_xmit.c
+++ b/core/rtw_xmit.c
@@ -4200,8 +4200,8 @@ s32 rtw_monitor_xmit_entry(struct sk_buff *skb, struct net_device *ndev)
 	if (unlikely(skb->len < rtap_len))
 		goto fail;
 
-	if (rtap_len != 12) {
-		RTW_INFO("radiotap len (should be 14): %d\n", rtap_len);
+	if (rtap_len < 8) {
+		RTW_INFO("radiotap len (should be >= 8): %d\n", rtap_len);
 		goto fail;
 	}
 
diff --git a/hal/rtl8821c/rtl8821c_ops.c b/hal/rtl8821c/rtl8821c_ops.c
index 1dc591b..6ad5712 100644
--- a/hal/rtl8821c/rtl8821c_ops.c
+++ b/hal/rtl8821c/rtl8821c_ops.c
@@ -852,6 +852,7 @@ static void hw_var_set_monitor(PADAPTER Adapter, u8 *val)
 {
 	u32	rcr_bits;
 	u8 mode;
+	u8 tmp_8bit;
 	HAL_DATA_TYPE *pHalData = GET_HAL_DATA(Adapter);
 	struct mlme_priv *pmlmepriv = &(Adapter->mlmepriv);
 
@@ -862,27 +863,35 @@ static void hw_var_set_monitor(PADAPTER Adapter, u8 *val)
 		return;
 	}
 
-	/* Receive all type */
-	rcr_bits = RCR_AAP | RCR_APM | RCR_AM | RCR_AB | RCR_APWRMGT | RCR_ADF | RCR_ACF | RCR_AMF | RCR_APP_PHYST_RXFF;
+	/* RCR: accept all addresses, append PHY status */
+	rcr_bits = RCR_AAP | RCR_APP_PHYST_RXFF;
 
 	#ifdef CONFIG_RX_PACKET_APPEND_FCS
 	/* Append FCS */
 	rcr_bits |= RCR_APPFCS;
 	#endif
 
-	#ifdef CONFIG_RX_PACKET_APPEND_CRC
-	/*CRC and ICV packet will drop in rx func*/
-	rcr_bits |= (RCR_ACRC32 | RCR_AICV);
-	#endif
-
 	rtl8821c_rcr_config(Adapter, rcr_bits);
-	/* Receive all data frames */
+
+	/* Configure HALMAC RX in PHY sniffer mode  this is critical
+	 * for beacon reception. Without this, the firmware filters
+	 * beacons before they reach USB.
+	 */
+	rtw_halmac_config_rx_info(adapter_to_dvobj(Adapter),
+		HALMAC_DRV_INFO_PHY_SNIFFER);
+
+	/* Enable PHY sniffer info append in RX descriptor */
+	tmp_8bit = rtw_read8(Adapter, REG_RX_DRVINFO_SZ_8821C);
+	rtw_write8(Adapter, REG_RX_DRVINFO_SZ_8821C, (tmp_8bit | 0x80));
+
+	/* Open all RX filter maps for MGMT, CTRL, and DATA */
+	rtw_write16(Adapter, REG_RXFLTMAP0_8821C, 0xFFFF);
+	rtw_write16(Adapter, REG_RXFLTMAP1_8821C, 0xFFFF);
 	rtw_write16(Adapter, REG_RXFLTMAP_8821C, 0xFFFF);
 
-	#if 0
-	/* tx pause */
-	rtw_write8(padapter, REG_TXPAUSE, 0xFF);
-	#endif
+	RTW_INFO("%s: monitor RCR=0x%08x DRVINFO_SZ=0x%02x\n",
+		 __FUNCTION__, rtw_read32(Adapter, REG_RCR_8821C),
+		 rtw_read8(Adapter, REG_RX_DRVINFO_SZ_8821C));
 }
 
 static void hw_var_set_opmode(PADAPTER Adapter, u8 *val)
@@ -899,9 +908,7 @@ static void hw_var_set_opmode(PADAPTER Adapter, u8 *val)
 
 	if (mode == _HW_STATE_MONITOR_) {
 		isMonitor = _TRUE;
-		/* set net_type */
 		Set_MSR(Adapter, _HW_STATE_NOLINK_);
-
 		hw_var_set_monitor(Adapter, val);
 		return;
 	}
@@ -1295,6 +1302,10 @@ static void hw_var_set_check_bssid(PADAPTER adapter, u8 enable)
 {
 	u32 rcr;
 
+	/* In monitor mode keep CBSSID cleared so beacons from all BSSIDs are received */
+	if (check_fwstate(&adapter->mlmepriv, WIFI_MONITOR_STATE))
+		return;
+
 	rcr = BIT_CBSSID_DATA_8821C | BIT_CBSSID_BCN_8821C;
 	if (enable)
 		rtl8821c_rcr_add(adapter, rcr);
@@ -1415,7 +1426,9 @@ static void hw_var_set_mlme_sitesurvey(PADAPTER adapter, u8 enable)
 		if (0 && MLME_IS_AP(adapter))
 			rcr_bit &= ~(BIT_CBSSID_BCN_8821C);
 
-		rtl8821c_rcr_add(adapter, rcr_bit);
+		/* In monitor mode keep CBSSID cleared so beacons from all BSSIDs are received */
+		if (!check_fwstate(&adapter->mlmepriv, WIFI_MONITOR_STATE))
+			rtl8821c_rcr_add(adapter, rcr_bit);
 #ifdef CONFIG_MI_WITH_MBSSID_CAM
 		/*if ((rtw_mi_get_assoced_sta_num(Adapter) == 1) && (!rtw_mi_check_status(Adapter, MI_AP_MODE)))
 			rtw_write8(Adapter, REG_BCN_CTRL, rtw_read8(Adapter, REG_BCN_CTRL)&(~DIS_TSF_UDT));*/
@@ -1480,7 +1493,9 @@ static void hw_var_set_mlme_join(PADAPTER adapter, u8 type)
 			val32 = BIT_CBSSID_BCN_8821C;
 		else
 			val32 = BIT_CBSSID_DATA_8821C | BIT_CBSSID_BCN_8821C;
-		rtl8821c_rcr_add(adapter, val32);
+		/* In monitor mode keep CBSSID cleared so beacons from all BSSIDs are received */
+		if (!check_fwstate(pmlmepriv, WIFI_MONITOR_STATE))
+			rtl8821c_rcr_add(adapter, val32);
 #endif
 		hw_bcn_func(adapter, _TRUE);
 		if (check_fwstate(pmlmepriv, WIFI_STATION_STATE) == _TRUE)
diff --git a/os_dep/linux/ioctl_cfg80211.c b/os_dep/linux/ioctl_cfg80211.c
index be72f10..0f978bd 100644
--- a/os_dep/linux/ioctl_cfg80211.c
+++ b/os_dep/linux/ioctl_cfg80211.c
@@ -3660,8 +3660,8 @@ static int rtw_cfg80211_monitor_if_xmit_entry(struct sk_buff *skb, struct net_de
 	if (unlikely(skb->len < rtap_len))
 		goto fail;
 
-	if (rtap_len != 14) {
-		RTW_INFO("radiotap len (should be 14): %d\n", rtap_len);
+	if (rtap_len < 8) {
+		RTW_INFO("radiotap len (too small): %d\n", rtap_len);
 		goto fail;
 	}
 
@@ -3698,10 +3698,8 @@ static int rtw_cfg80211_monitor_if_xmit_entry(struct sk_buff *skb, struct net_de
 
 		return ret;
 
-	} else if ((frame_ctl & (RTW_IEEE80211_FCTL_FTYPE | RTW_IEEE80211_FCTL_STYPE))
-		== (RTW_IEEE80211_FTYPE_MGMT | RTW_IEEE80211_STYPE_ACTION)
-	) {
-		/* only for action frames */
+	} else if ((frame_ctl & RTW_IEEE80211_FCTL_FTYPE) == RTW_IEEE80211_FTYPE_MGMT) {
+		/* all management frames: action, deauth, auth, probe, assoc, ... */
 		struct xmit_frame		*pmgntframe;
 		struct pkt_attrib	*pattrib;
 		unsigned char	*pframe;
@@ -3715,23 +3713,25 @@ static int rtw_cfg80211_monitor_if_xmit_entry(struct sk_buff *skb, struct net_de
 		u8 category, action;
 		int type = -1;
 
-		if (rtw_action_frame_parse(buf, len, &category, &action) == _FALSE) {
-			RTW_INFO(FUNC_NDEV_FMT" frame_control:0x%x\n", FUNC_NDEV_ARG(ndev),
-				le16_to_cpu(((struct rtw_ieee80211_hdr_3addr *)buf)->frame_ctl));
-			goto fail;
-		}
+		if ((frame_ctl & RTW_IEEE80211_FCTL_STYPE) == RTW_IEEE80211_STYPE_ACTION) {
+			if (rtw_action_frame_parse(buf, len, &category, &action) == _FALSE) {
+				RTW_INFO(FUNC_NDEV_FMT" frame_control:0x%x\n", FUNC_NDEV_ARG(ndev),
+					le16_to_cpu(((struct rtw_ieee80211_hdr_3addr *)buf)->frame_ctl));
+				goto fail;
+			}
 
-		RTW_INFO("RTW_Tx:da="MAC_FMT" via "FUNC_NDEV_FMT"\n",
-			MAC_ARG(GetAddr1Ptr(buf)), FUNC_NDEV_ARG(ndev));
-		#ifdef CONFIG_P2P
-		type = rtw_p2p_check_frames(padapter, buf, len, _TRUE);
-		if (type >= 0)
-			goto dump;
-		#endif
-		if (category == RTW_WLAN_CATEGORY_PUBLIC)
-			RTW_INFO("RTW_Tx:%s\n", action_public_str(action));
-		else
-			RTW_INFO("RTW_Tx:category(%u), action(%u)\n", category, action);
+			RTW_INFO("RTW_Tx:da="MAC_FMT" via "FUNC_NDEV_FMT"\n",
+				MAC_ARG(GetAddr1Ptr(buf)), FUNC_NDEV_ARG(ndev));
+			#ifdef CONFIG_P2P
+			type = rtw_p2p_check_frames(padapter, buf, len, _TRUE);
+			if (type >= 0)
+				goto dump;
+			#endif
+			if (category == RTW_WLAN_CATEGORY_PUBLIC)
+				RTW_INFO("RTW_Tx:%s\n", action_public_str(action));
+			else
+				RTW_INFO("RTW_Tx:category(%u), action(%u)\n", category, action);
+		}
 
 dump:
 		/* starting alloc mgmt frame to dump it */
@@ -4620,6 +4620,38 @@ static int cfg80211_rtw_set_monitor_channel(struct wiphy *wiphy
 	return 0;
 }
 
+static int cfg80211_rtw_get_channel(struct wiphy *wiphy, struct wireless_dev *wdev,
+				    struct cfg80211_chan_def *chandef)
+{
+	_adapter *padapter = wiphy_to_adapter(wiphy);
+	struct ieee80211_channel *chan;
+	enum nl80211_band band;
+	u32 freq;
+	u8 ch;
+
+	if (!padapter)
+		return -EINVAL;
+
+	ch = rtw_get_oper_ch(padapter);
+
+	if (ch <= 14)
+		band = NL80211_BAND_2GHZ;
+	else
+		band = NL80211_BAND_5GHZ;
+
+	freq = ieee80211_channel_to_frequency(ch, band);
+	chan = ieee80211_get_channel(wiphy, freq);
+	if (!chan)
+		return -EINVAL;
+
+	chandef->chan = chan;
+	chandef->width = NL80211_CHAN_WIDTH_20;
+	chandef->center_freq1 = freq;
+	chandef->center_freq2 = 0;
+
+	return 0;
+}
+
 static int	cfg80211_rtw_auth(struct wiphy *wiphy, struct net_device *ndev,
 		struct cfg80211_auth_request *req)
 {
@@ -6846,6 +6878,7 @@ static struct cfg80211_ops rtw_cfg80211_ops = {
 	.leave_ibss = cfg80211_rtw_leave_ibss,
 	.set_tx_power = cfg80211_rtw_set_txpower,
 	.get_tx_power = cfg80211_rtw_get_txpower,
+	.get_channel = cfg80211_rtw_get_channel,
 	.set_power_mgmt = cfg80211_rtw_set_power_mgmt,
 	.set_pmksa = cfg80211_rtw_set_pmksa,
 	.del_pmksa = cfg80211_rtw_del_pmksa,
diff --git a/os_dep/linux/ioctl_linux.c b/os_dep/linux/ioctl_linux.c
index b789944..defc9da 100644
--- a/os_dep/linux/ioctl_linux.c
+++ b/os_dep/linux/ioctl_linux.c
@@ -1803,9 +1803,10 @@ static int rtw_wx_get_freq(struct net_device *dev,
 		wrqu->freq.i = pcur_bss->Configuration.DSConfig;
 
 	} else {
-		wrqu->freq.m = rtw_ch2freq(padapter->mlmeextpriv.cur_channel) * 100000;
+		u8 ch = rtw_get_oper_ch(padapter);
+		wrqu->freq.m = rtw_ch2freq(ch) * 100000;
 		wrqu->freq.e = 1;
-		wrqu->freq.i = padapter->mlmeextpriv.cur_channel;
+		wrqu->freq.i = ch;
 	}
 
 	return 0;